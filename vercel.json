{
    "version": 2,
    "builds": [
        {
            "src": "api/polly.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/$1"
        }
    ]
}
